> *This was generated by AI during triage.*

# 创建 IJP 溢流监控报表

Category: `enhancement`
Status: `ready-for-agent`
Created: `2026-09-01`
Source: `docs/dev_docs/dev_spec/qtime_domain/task-IJP溢流报表开发.md`
Related: `docs/dev_docs/dev_spec/qtime_domain/sql-IJP溢流报表开发.md`、`docs/dev_docs/dev_spec/qtime_domain/IJP溢流报表样式-开发界面.png`、`docs/dev_docs/dev_spec/qtime_domain/datasource-IJP溢流报表分析.md`

## Current context

- 内部已存在一份基于 FineReport 的 IJP 溢流监控报表（IJP = OLED 蒸镀段 TFE 有机层打印工艺；"溢流"指墨水溢出类缺陷，RS_CODE 白名单 C3DM0~C3DM5/C3RA1~C3RA3/C3ZC1/C3BH1/C3BH2），仓库内没有对应的 Python + Streamlit 实现。
- FineReport 开发界面给出的结构：筛选区（工厂、开始/结束时间、产品名称、工单类型、线体、设备、产品型号、GlassID、CODE、边框、Target值、批次、Cycle）+ `OLED RS Overflow By天` 堆叠占比柱状图 + 明细表（Print Time / ProductCode / Glass ID / Printer / Panel ID / 原图超链接 / Panel Location / CODE_RATIO）。
- 数据集 SQL 共 13 组（CYCLE/FACTORY/PICI/PRODCODE/PRODUCT/SEARCH_SUBPRODTYPE 筛选项；SEARCH/SERACH1 明细；SEARCH_BYBORDER/SEARCH_BYBORDER1 按边框占比；SERACH_BYDAY 按天占比；SEARCH_TOTAL/SERACH_TOTAL2 按设备/产品 Total 占比）。
- 只读数据库探查（`output/tmp/ijp_db_probe_result.md`）确认全部数据源可访问：核心视图 `EDA.SPOT_EDA_OLED_VIEW_DFT_V`、设备履历 `EDA.OLED_CHAMBER_HST_T`（5 台 IJP 设备白名单全部存在）、`DWR_MES_PRODUCTSPEC`（明细 JOIN 用基础表）、`DWR_MES_PRODUCTSPEC_V`（筛选项用视图）、`DWR_MES_PRODUCTREQUEST_V`、`EDA.DWD_GLASS_OLED_CYCLE_V3`、`DWR_MES_MESFACTORY_V`。最近 30 天窗口白名单过滤后约 30 万行。
- 已确认的关键语义偏差：`GLASS_START_TIME`/`CUT_START_TIME` 为 timestamp（不可做 `<> 'NaT'` 判断）；`EVENT_TIME` 为 varchar（保留 `::TIMESTAMP` + `<> 'NaT'`）；`DWR_MES_PRODUCTSPEC` 基础表与 `_V` 视图列不同，不可互换。
- 页面必须遵守薄入口、section、图表适配器的展示边界；后端落在既有 `src/qtime_domain/` 子域内（IJP 属 OLED 段过货/质量监控族），页面不直接访问数据库，所有过滤使用绑定参数。

## Desired outcome

- 新增仓库内 IJP 溢流监控 Streamlit 页面，用户可选择时间范围及各项筛选（产品名称/产品型号/工单类型/线体/设备/GlassID/CODE/边框/批次/Cycle）后执行查询。
- 查询结果以参考样式呈现：`OLED RS Overflow By天` 按天 × CODE 的堆叠占比柱状图（含可选 Target 参考线），以及明细表（Print Time、ProductCode、Glass ID、Printer、Panel ID、原图链接、Panel Location、CODE 占比）。
- 数据访问使用参数化 SQL，正确处理空筛选（不过滤）、非法时间范围、空结果和数据库错误，不泄露连接信息或 SQL 细节。

## Acceptance criteria

- [ ] 页面可由 Streamlit 原生 pages 机制自动发现并打开，入口为薄组合层（sys.path 注入 + set_page_config + AppSetup + 页头 + section 委托）。
- [ ] 筛选语义与 FineReport `IF(LEN(...)=0,"",...)` 一致：任一筛选为空表示不过滤；时间范围为闭区间 `[start, end]`（与原 SQL 一致）；查询由「查询」按钮门控，不自动加载。
- [ ] 设备过滤固定为 5 台 IJP 设备白名单（3CEE01-IK2-PR1/PR2、3CEE02-IK2-PR1/PR2、3CEE04-IKT-PRT）；RS_CODE 固定为 12 个白名单；线体 = SUB_EQUIP_ID 前 6 位。
- [ ] PANEL_ID 按 `SUBSTRING(RS_DEFECT_IMAGE_NAME, 57, 14)` 提取；PANEL_LOCATION 按图片名后缀映射（TOP/BOTTOM/LEFT/RIGHT/四角，BOTTOM 细分 BOTTOM0~9，非 C3DM% 映射 KONG* 系列）；原图列展示 `http://10.73.17.41/IMG_WEB/V3/` 前缀拼接的可点击链接。
- [ ] By天 图为按天堆叠的 CODE 占比（占比 = 当组 CODE 数 / 当组总数，ROUND 3 位），Target 值输入作为图上参考线；空数据、数据库错误均有安全明确的展示。
- [ ] 明细表含参考样式全部列，CODE_RATIO 按 GLASS_ID 内 CODE 数 / GLASS_ID 总数计算（ROUND 3 位），并给出 Total 汇总行。
- [ ] 所有 SQL 使用绑定参数（IN 列表用 expanding bindparam），不拼接用户输入；数据库失败时抛出不含 SQL/凭据/traceback 的稳定错误文案。
- [ ] 单元/集成测试覆盖 DTO 校验、筛选组合 SQL 契约（SQLite ATTACH 模拟多 schema）、聚合/位置映射逻辑、图表模型与错误/空状态；浏览器 E2E 使用隔离 fixture 验证筛选、查询、图表与表格关键流程并通过。
- [ ] 既有页面与测试无新增回归；不修改用户已有资源文件与任务文档。

## Out of scope

- 变更目标数据库表结构、权限或修改 FineReport 原报表。
- 复刻 FineReport 自带的分页、打印、邮件、导出工具栏。
- 缺陷原图的图片内容代理/鉴权服务（仅渲染既有 IMG_WEB 链接）。
- 快照降级契约（与 Q-Time 域现状一致，数据库失败只显示稳定错误）。

## Questions to resolve

- [x] 数据源与字段映射：全部经只读探查验证（见 Related 分析文档），无需降级源。
- [x] timestamp 列的 `<> 'NaT'`：仅保留在 varchar 的 EVENT_TIME 上。
- [x] 空筛选语义：空 = 不过滤（沿用 FineReport 语义）。
- [x] Target 值无独立数据源：复刻为 By天 图的参考线输入。
- [x] 时间边界：沿用原 SQL 闭区间 `>= / <=`；By天 数据集起始时间向前扩 7 天。

## Agent Brief

**Category:** enhancement
**Summary:** 在 `src/qtime_domain/` 内新增 IJP 溢流监控子域（DDD 分层）与 Streamlit 页面，复刻 FineReport 的多维筛选、By天 CODE 占比堆叠图（含 Target 参考线）和缺陷明细表。

**Current behavior:** 仓库内无 IJP 溢流报表实现；FineReport 原版提供 13 组数据集 SQL。全部数据源已经只读探查验证可访问（`docs/dev_docs/dev_spec/qtime_domain/datasource-IJP溢流报表分析.md`）。

**Desired behavior:** 用户在新页面选择时间范围与可选筛选（产品名称/型号、工单类型、线体、设备、GlassID、CODE、边框、批次、Cycle），点击查询后看到 `OLED RS Overflow By天` 堆叠占比图与明细表；空筛选表示不过滤；错误与空数据有明确稳定提示。

**Key interfaces:**

- `IjpQuery`（pydantic frozen DTO）：时间范围（必填，start<=end）+ 可选筛选字段（空列表/空串 = 不过滤）+ target 值（可选数值）。
- `IjpDataPort`（Protocol）：`list_filter_options(...)`（级联选项：产品型号、产品名称、工单类型、批次、Cycle）；`fetch_daily_ratios(query)`（By天 占比聚合）；`fetch_details(query)`（明细，限量）。
- 仓储适配器走 `DatabaseManager` 单例 + `sqlalchemy.text()` 绑定参数（IN 用 expanding bindparam）；任何异常包装为 `IjpDataAccessError`（稳定中文文案，不含 SQL/凭据）。
- 固定白名单：5 台 IJP 设备、12 个 RS_CODE；线体 = 设备号前 6 位；PANEL_LOCATION 映射与 BOTTOM0~9 细分、KONG* 系列按原 SQL。

**Edge cases:** 空筛选组合（全量）；时间范围倒置（DTO 校验拒绝）；空结果；数据库不可达/权限不足；RS_DEFECT_IMAGE_NAME 长度不足或后缀无法映射（PANEL_LOCATION 为 NULL）；明细超量时限量截断并提示。

**Verification:** 单元测试（DTO、映射、服务、图表/表格模型）+ 集成测试（SQLite ATTACH 模拟 EDA/DWR_MES schema，验证 SQL 契约）+ E2E（隔离 fixture 页面 + playwright-cli，产物落 `output/test-results/ijp/`）；既有测试无回归。

## Comments

### 2026-09-01 — Initial note

> *This was generated by AI during triage.*

- 依据任务书完成样式截图与 13 组数据集 SQL 的逐项分析，并对全部数据源执行只读探查（探查脚本与结果在 `output/tmp/ijp_db_probe*.py/.md`），7 项验证全部通过。
- 数据源分析文档已输出：`docs/dev_docs/dev_spec/qtime_domain/datasource-IJP溢流报表分析.md`，含与原 SQL 的三处必要偏差说明（timestamp 类型、基础表 vs 视图、线体/CODE/边框选项可由白名单与枚举派生）。
