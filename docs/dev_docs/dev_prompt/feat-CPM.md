# CPM监控报表

## Background
你好，我是一家OLED制造厂的员工，该项目是一个用于给客户展示厂内数据的报表。
现在需要开发一个SPC的CPM监控报表。

## Terms
0. LOT/SHEET/GLASS/PANEL：
    - Lot：生产单元，一般一个lot包含30个sheet。ID为9位。
    - Sheet：显示屏在前段工艺中是一整个大板，我们称之为Sheet。ID为11位，前9位为Lot ID。
    - Glass：Sheet在蒸镀工艺段被一切为二。ID为12位，前11为Sheet ID。
    - Panel：在屏体段（最后一道工艺）大板才会被切割成手机尺寸的大小，切割后的屏体我们称之为Panel。ID为15位，前11为SheetID，后四位为坐标。
1. Mapping：将每一个Panel根据位置坐标拼接成一整个大板来寻找前端工艺的问题，是一种异常调查的常见手段。而拼接成的大板就叫做Mapping。注意，这里的拼接是在数据维度上，因此可以根据需求拼接。
    - 膜位：Mapping图上Panel所在的位置就叫做膜位。比如我们可以将所有的panel拼接成Mapping图，来观察哪个膜位的不良较为显著。
2. Defect Code：不良代码
3. 监控指标：“站点-参数”的组合。每个站点可能会测多个参数，但每个参数可能会在多个站点测量，所以只有组合才能锁定唯一一个监控指标
4. SPC：统计过程控制。对于我们公司来说，具体形式为：
    - 针对每个监控指标，在一个sheet上的多个点位进行测量，取均值作为该sheet的测量值。
    - 每个指标都有上下限，通过“自动预警报表”进行监控。

---

# Task1：CPM/CPK-ByLot

## Workflow
现在，我们需要统计每个Lot在每个监控指标上的CPM，大致步骤如下：
针对每个SPC监控指标
1. 每个计算出每个sheet的测量值（spc_domain已经实现）
2. 计算每个LOT的CPM（CPM的计算公式您查询一下即可得到）
    - lot与sheet的之间归属关系为：Sheet ID的前9位为其所属的lot ID
3. 绘制Lot别趋势推移图（可参考“入库不良率分析看板”中的lot趋势图）

## Requirements
1. Backend：请在spc_domain中来编写后端程序，其中逻辑您可以复用
    - 请注意，我们只针对SPC指标计算CPM。不包括AOI和CTQ。
2. Frontend：新建页面要求如下
    - 筛选框：产品型号，厂别，参数名称，站点
        * 产品型号：参考自动预警看板
        * 厂别：仅包含ARRAY/OLED/TP
        * 参数名称：提供全选按钮，默认为空。
        * 站点：提供全选按钮，默认为空。跟随“参数名称”联动，即选择“参数名称”后，“站点”筛选框下仅
    - 引入Header组件（render_page_header）
    - 每个监控指标放入一个Expander中，默认展开。增添全部展开/折叠的按钮。
    - 点击lot可查看该lot下所有sheet的测量值（联动关系可参考“入库不良率分析看板”中的lot趋势图）

## Goal
1. 请制定计划，包含一份详细的Checkliet
2. 请进行开发，并执行冒烟测试（可以调用Playwright MCP）直至所有功能都能够正常实现
    - 为了便于测试，您可以先截取近七天的数据作为测试数据，避免数据量过大导致运行缓慢

## Task1-Fix
1. 请去掉全选和清空按钮，我发现Streamlit的多选下拉框已经自带了全选与清空功能。
2. lot别趋势图，不要绘制成折线图，而是柱状图。
3. 去掉“全部折叠”、“全部展开”的按钮。
4. 筛选框逻辑调整：
    - 取消“产品型号”：“产品型号”由Header筛选即可。
    - 增加“厂别-站点”联动关系：将厂别设置为单选，站点保持多选。选择厂别后，站点只能选择相应厂别下的“站点”
    - 新增“站点-参数”联动关系：选择站点后，自动全选所选站点下所有参数
    - 查询：当所有参数选择完毕后再生成图片，或者增加“查询”按钮，点击后再生成图片。
5. 除此之外，其它基本符合要求，直接应用近三个月的数据并上线。
    - 开始日期截止至三个月前的1号。比如今天是2026-06-16，那么开始日期就是2026-04-01
6. 最后检查并优化一下界面排布

---

# Task2：CPM/CPK-By月周天 + SPC箱线图
抱歉，以lot别统计CPM似乎并无意义。我们需要彻底重构这个看板。

从以下两个维度统计并绘制对应箱线图（不要绘制具体数据点）：
1. 月周天箱线图
2. sheet级数据箱线图
 
## Figure1
包含以下两幅图像，绘制在同一个窗口内：
1. 根据每个监控指标的sheet别数据（每个sheet取sheet_mean作为测量值），绘制月周天箱线图：
    - 默认两月（从上个月第一天开始，包含上个月和本月）、三周（从三周前的第一天开始，近三周）、七天（近七天）。
    - 样式：可参考图一
        * 标出UCL/LCL/USL/LSL，并以USL和LSL作为上下限
        * 月周天的箱线图之间要有明显分割。
2. 针对每月、每周、每天，计算CPM和CPK并绘制折线图：
    - 为CPK绘制规格线（1.33），CPM不需要。
    - 样式：可参考图一。月周天的折线不要连在一起。

## Figure2
sheet级数据箱线图（根据所有测试点位绘制），但有两种排序模式：
1. 根据腔室区分，并按照腔室排序
    - 样式：请参考图二。不同腔室的箱线图使用不同颜色
2. 按照过货时间排序

## Selector
逻辑基本保持不变，产品型号由Header component控制
1. 指标：单选框；备选项有“CPM、CPK”，默认为CPM
2. 厂别：单选框
3. 站点：多选框
4. 参数名称：多选框，依旧保留选择站点后，自动选择所有对应参数的逻辑

## Task2-fix
谢谢。针对该模块（SPC+CPM/CPK），我们需要优化以下几点：
1. 对于“Sheet 点位箱线图”（_create_sheet_points_box_chart），用于区分腔室的字段不是“site_name”，而是“unit_id”（匹配前六位，或者“-”之前）。
    - 示例：unit_id可能的取值为“3CEE02-PPA”，它的腔室为“3CEE02”
2. 当前的两月、三周、七天是连续的，因此当中间某一段时间没有过货时，就会出现中间空白的情况（如图一所示）。能否向前补全，确保有两月、三周、七天的数据？（如图二所示，尽管其中的时间点并不是连续的）
3. 给CPM/CPK点标上具体的数据
4. 对于Sheet 点位箱线图，“按腔室排序”和“按过货时间排序”不再用下拉框切换，而是并排显示（如图三）。 
    - 现在每一行应该是三幅图并排：月周天箱线图、Sheet 点位箱线图（By腔室）、Sheet 点位箱线图（By过货时间）

## Task2-fix
谢谢。但需要继续修正：
1. 月周天箱线图：对于图一，CPM/CPK的折线图也需要对齐箱线图，向前追溯完成补全
2. Sheet 点位箱线图-By腔室：您已经找到了正确的腔室号，但我们要绘制的依旧是Sheet 点位箱线图，只是根据腔室号进行排序，不同腔室根据颜色区分。
    - 您可以理解为我们只是对“Sheet 点位箱线图-By过货时间”进行了重排序和颜色标准
3. 如果有箱线图的超出了规格线（USL/LSL），那么图片的上下限应该自适应调整，能够展示出超规片，而不是继续使用规格线（USL/LSL）作为图片的上下限

## Task2-feat：数据修饰
谢谢。请继续添加修饰逻辑，针对Sheet 点位箱线图中的数据，应当自动修正：
1. 当识别到有sheet超规（USL/LSL）之后，则根据产品型号输出一份文件到对应的路径下：“D:\wzy\Python\vivo-project\resources\{产品型号}”
    - 路径示例：对于“Z571”产品，其对应的路径为“D:\wzy\Python\vivo-project\resources\Z571”
2. 该文件中的内容包括所有的超规片及其相应信息。必须有的字段是：
    - 厂别
    - 产品名称（prod_code）-站点（step_id）-参数名称（param_name）-sheet_id：这四个字段锁定唯一“超规片”
    - 过货时间（sheet_start_time）
    - 该sheet对应的max\min\mean
3. 对于所有超规片，默认执行使用规格线（USL/LSL）内的一个随机值进行截断
    - 对于USL，就是使用USL下方的一个随机值截断，对于LSL，就是使用LSL上方的一个随机值截断
4. 在对应的产品路径（“D:\wzy\Python\vivo-project\resources\{产品型号}”）中，再添加一份修饰文件：字段与step2中的表对应，但增添一个flag字段。如果flag字段为False，则不再对该记录进行截断
    - 匹配字段：产品名称（prod_code）-站点（step_id）-参数名称（param_name）-sheet_id
    - 每个产品的数据受相应路径下的修饰文件的约束
5. 前端修改：请仿照“app\pages\入库不良率分析看板.py”中的“render_trend_override_uploader”，创建一个超规片明细表下载，和修正表的上传的功能。分别用于给用户查看超规片明细，和对指定的超规片进行修正
    - 以上功能应该只有在url中有“?admin-true”的情况下开启

【Hint】您可以根据实际情况对以上方案进行相应优化，只要实现以下两项功能：
1. 超规片默认修正
2. 可以根据用户配置展示真实数据

## Task2-fix：前端修饰
1. 在一些月周天箱线图中，会显示“当前监控指标可用于箱线图展示，但周期 CPM/CPK 样本不足。”这是什么意思
2. 抱歉，当前CPM和CPK的折线图加上后，看起来过于混乱，因此我们需要做出如下修正：
    - 取消筛选框中的“CPM/CPK”
    - 月周天的CPM/CPK不再使用折线图显示，而是使用表格同时展示出来。呈现在每个Expander中三个图像的上方
3. 将每个Expander中的提示字段，改为如下四个值：“中位CPK”、“最小CPK”、“中位CPM”、“最小CPM”

## Task2-fix：与“自动预警看板”同步
1. 以上修正后的数据应当同样适用于“自动预警看板.py”
    - 按照当前设计，“自动预警看板.py”与“CPM监控报表.py”中的数据源完全一致，能否在后端实现数据修饰的逻辑，然后同时提供给两个报表呈现数据？

## Task2-fix：SPC箱线图优化
1. 请优化数据修饰逻辑：当前SPC箱线图默认使用规格线截断，我要求能够针对特殊的参数进行特殊优化。
    - 对于名称（param_name）中包含“PPA”的参数，截断线一律设为（lsl-0.5，usl+0.5）
    - 这一功能应该可配置，配置文件路径：“config\spc_config.yaml”
2. 对于月周天箱线图，目前使用的是sheet_mean，请转为使用对应时间段（月、周、天内的）所有的测试点位数据来绘制箱线图。
    - 但请仿照“D:\wzy\Python\vivo-project\src\shared_kernel\config.py”中的“get_cpm_period_sigma_source”，使其同样可配置

## Task2-fix：SPC箱线图排布优化
1. 当前三幅图并列排布的模式，在小显示屏上看起来会非常拥挤，请进行修改：每个Expander内分为三行
    - 第一行：月周天箱线图和CPK/CPM明细表并列（CPK/CPM改为列字段，月周天改为行字段）
    - 第二行：“Sheet点位分布 By腔室”
    - 第三行：“Sheet点位分布 By过货时间”

## Task2-fix：汇总图表优化
请增添CPK预警功能：自动计算出所有“最小CPK＜1.33”的超规项明细（厂别-站点-参数-参数名称-超规日期-cpk值），并显示在界面上方：
1. 在筛选框上方新增一个Expander，将报警信息放入其中。你可以参考"app\pages\入库不良率分析看板.py"中的“render_alert_center”
2. 当前数据应该是在点击“查询”按钮后才会加载并渲染图表。而本功能要求在Header选中一个产品后立刻加载（Header有默认值，相当于进入页面后就需加载）。因此点位数据、sheet_mean和cpk数据都会预先计算并加载。为了避免反复计算，能否考虑在这些数据加载或计算后直接缓存，后续渲染图像时直接使用
