接下来请按照以下方案继续完成合并：

## 3.1 语义冲突
- master与本分支"只写 __flags、绝不碰明细"的持久化不变量正面矛盾：保留本分支的模式
- clip_rules 链路必须整条摘除：是的
- excel_tools.py 两套原子写实现择优：可以，采用分支的方案
- master 把修饰工作簿迁到 resources/inline_domain/：这一点要保留master的方案

## 3.2-3.4
至于其它冲突，请先采用以下准则判断，然后再迁移
- 所有的架构决策都采用master分支
- 所有与“sheet明细持久化的功能”相关的程序决策都采用本分支
- 所有与“sheet明细持久化的功能”无关的程序决策都采用master分支

如果你发现某处迁移会导致以上判断准则的冲突，可以暂停向我询问