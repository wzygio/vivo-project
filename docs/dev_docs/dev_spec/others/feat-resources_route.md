# 文件简化
当前`resources`路径下的文件太过复杂，不利于维护，请进行以下修正

1. 将ctq文件的输出路径转移至每个产品的文件夹下。例如从`resources\M626\ctq`转移至`resources\M626\`
2. 将每个文件的产品下的`resources\M626\analysis_files`和`resources\M626\project_files`汇总到一起并输出至根目录下。