我们需要对该报表进行优化：
1. 对于能够从快照中提取到数据的参数，我们使用数据快照中的值
2. 但是对于无法从快照中提取到数据的参数，我们则采用数据仿造出的快照中的值
3. 但在此之前，我们需要先优化一下数据仿造的逻辑：原始数据集的仿造，和仿造数据集的更新应该是两个程序、两套逻辑。
    - 数据生成：测量值应该是随机取规格线的0%到100%之间的值。测量时间使用近两天的随机时间。
    - 更新机制：快照有效期24小时，重新生成时，原有的测量时间加一天，原有的测量值加规格线的30%。如果超过规格线100%，则重新从规格线30%以内随机取一个数字。