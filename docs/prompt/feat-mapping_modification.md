# Mapping Modification

## Background
你好，我是一家OLED制造厂的员工，该项目是一个用于给客户展示厂内数据的报表。
由于是给客户展示，所以需要修饰数据来避免问题暴露。接下来我想让你帮助我开发入库不良率（yield_domain）中的Mapping修饰逻辑。

## Term
1. Mapping：
- Sheet：显示屏在前段工艺中是一整个大板，我们称之为Sheet。
- Panel：在屏体段（最后一道工艺）大板才会被切割成手机尺寸的大小，切割后的屏体我们称之为Panel。
- Mapping：将每一个Panel根据位置坐标拼接成一整个大板来寻找前端工艺的问题，是一种异常调查的常见手段。而拼接成的大板就叫做Mapping。注意，这里的拼接是在数据维度上，因此可以根据需求拼接。
- 膜位：Mapping图上Panel所在的位置就叫做膜位。比如我们可以将所有的panel拼接成Mapping图，来观察哪个膜位的不良较为显著。
2. Defect Code：不良代码

## Requirements
我们需要以下四种逻辑：
1. 展示原始分布
2. 晕染：将每个膜位向正负两个膜位的范围内分散。
3. 随机分布（保证不良数一致，但分布完全随机，看不出任何集中性）
4. 自定义修饰

## Task
1. 当前晕染和自定义修饰的逻辑已经实现。请你思考并开发随机分布的逻辑。
2. 请开发一套配置系统，可以让我自由地配置使用哪种修饰逻辑，精细度如下：
产品 - Defect Code - 批次，支持ALL

## Goal
请你不断优化开发，直至以下测试用例通过：
当前“M626-彩斑Mura-0429批次”的Mapping图中，2E行存在明显集中性（如图所示，你可以通过对每一行进行加和来判断）。您需要：
1. 使用新开发的随机分布算法消除这种集中性。
2. 精准地应用于“M626-彩斑Mura-0429批次”的数据

## Addtional Task
完成上述任务后，请顺带修改当前Mapping图的色系，缩小色系跨度，不要再出现深红色

---

## Task1-fix
谢谢。不过需要修改：
1. 当前对于mapping修饰的配置过于复杂。我建议将其改为一个xlsx配置文件。
- 请扫描所有产品的配置，将所有有关mapping的配置都抽取到该配置文件中，新增“产品型号”用于区分。
- 它应该支持我的“自定义修饰算法”的相关配置。
2. 同时，让我的“自定义修饰算法”也能够精细到对“产品-Code-蒸镀批次”进行控制。
- 产品-Code-蒸镀批次，支持ALL通配字符，不填写则默认为ALL。
3. 在前端页面中的配置文件上传组件（render_trend_override_uploader）中，新增mapping_config的对应的选项卡。
4. 如果成功，请将当前“自动预警”模块（spc_domain）的相关配置机制（config\compliance_config.yaml）也改为使用xlsx配置文件。

---

## Task2
谢谢。请进一步优化：
1. 不要增加规格线并区分颜色。
2. 明细表中，去掉我用红框框出来的字段，仅保留lot_id、sheet_id（这里我框错了，该字段需要保留）、厂别、站点、参数名称、sheet_mean即可。
3. 解决以上问题后，当前页面接近完美，请仿照其绘制一个CPK报表。样式和规则原封不动，只要更换计算公式即可。并在最前面新增一个选择器用于切换CPM和CPK。