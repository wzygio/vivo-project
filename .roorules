# 🌍 天柱专项报表系统

本项目是一个面向面板制造业（OLED/Array 半导体显示）的良率（`Yield`）分析与统计过程控制（`SPC`）报表系统。

---

## 📖 请务必先阅读项目规范

```bash
spec/00_project_spec.md
```

该文件包含：词汇表、业务框架（Yield/SPC/Shared Kernel）、系统框架（技术栈/配置/缓存/日志/文件处理/安全）、开发框架（EPCC Flow / TDD / 防御性编程）、业务边界。

---

## 🚨 全局红线纪律（每次修改前必读）

以下为硬性约束，任何时候**不得违反**：

1. **禁止静态重构核心算法**：良率模拟（`_simulate_concentration`、`_distribute_sheet_from_lot`）和 Mapping 级联衰减经过多次业务验证，静态重构会破坏已修复的边界条件。
2. **禁止修改数据库连接单例**：`DatabaseManager`（`__new__` 单例 + 失败重试）是容灾基石，任何修改会破坏 `.env` 延迟加载和断线重连。
3. **禁止移除 `@st.cache_data`**：L2 缓存移除将导致每次刷新触发全量数据库查询，造成性能灾难。
4. **禁止简化 Parquet 快照增量更新**：TTL 保护 + 2 天缓冲 + 三防线容灾是精心设计的降级策略。

---

## 🔧 遇到特殊问题？查 Skills 库

```bash
skills/README.md
```

记录了加密 Excel 读取（COM）、类型标准化匹配等专项解决方案。

---

## ⚡ 快速命令

```bash
# 启动
start_streamlit.bat

# 测试（必须 100% PASS）
uv run pytest tests/ -v --tb=short
```
